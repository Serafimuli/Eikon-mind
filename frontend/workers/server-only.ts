// Next.js resolves `server-only` to an empty module in server bundles.
// The scheduled maintenance Worker runs server-side but is bundled directly
// by Wrangler, so it uses this equivalent marker implementation.
